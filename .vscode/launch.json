{        
    "configurations":[{
            "type": "lldb",
            "request": "launch",
            "name": "Launch llvm",
            "program": "${workspaceFolder}/eva-llvm",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}